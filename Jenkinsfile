node ('snakeserver'){  
    def app
    stage(' Build') {
        /* Let's make sure we have the repository cloned to our workspace */
       echo "Build"
    }  
    stage(' Test') {
        /* Let's make sure we have the repository cloned to our workspace */
       echo "test"
    }  
    stage(' deploy') {
        /* Let's make sure we have the repository cloned to our workspace */
       echo "deploy"
    }  
    
 
}
